import SwiftUI

/// Main app entry point.
@main
struct DiraigentApp: App {
    @State private var appState = AppState()

    var body: some Scene {
        WindowGroup {
            ContentView()
                .environment(appState)
        }
    }
}
